apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "push_gateway.fullname" . }}-test-connection"
  labels:
{{ include "push_gateway.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "push_gateway.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
